extends layout.pug 

block content
  main
    section.search-page
      .search-page__wrapper
        h1 Поиск
        div(class=`search  search-page__form  ${articles && articles.length === 0  ? `search-page__wrapper--empty` : ``}`)
          form(action='/search', method='get')
            label
              input(type='text', placeholder='Что ищем?', name='search')
            button.search__button.button.button--transparent(type='submit') Найти
          if articles && articles.length > 0
            each article in articles
              .search__result
                time.search__date(datetime=`${article.createdDate.replace(/\s/, `T`)}`) #{article.createdDate.replace(/-/g, `.`).replace(/\s/, `, `)}
                a.search__link
                  | #{article.title}
        if articles && articles.length === 0
          p.search-page__empty Ничего не нашлось